# Kconfig поверх corne.conf — только для раскладки instant.
#
# Пусто намеренно. ru-layer-sync выключен по умолчанию, и включать его здесь
# нельзя: слой задаётся индексом, а слоя RU в instant нет — значение по
# умолчанию (1) попало бы в NAV, и смена языка на хосте включала бы навигацию.
#
# Файл существует, чтобы -DEXTRA_CONF_FILE в Makefile был одинаков для
# всех раскладок.
