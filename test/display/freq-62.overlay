/*
 * 62.5 кГц — ниже штатной сетки SPIM (125k/250k/500k/1M), nRF округлит вверх
 * до 125 кГц. Файл оставлен как явная нижняя ступень для перебора.
 */

&nice_view {
    spi-max-frequency = <62500>;
};
